PORT=3000
NODE_ENV=development

# OpenAI API Key (OPCIONAL)
# Agora você pode configurar a chave diretamente na interface web.
# Este valor serve apenas como fallback se nenhuma chave for fornecida via interface.
# OPENAI_API_KEY=your_openai_api_key_here
